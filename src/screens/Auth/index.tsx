import React from 'react';
import {View, Text} from 'react-native';
import {styles} from './styles';

const AuthScreen = () => {
  return (
    <View style={styles.screen}>
      <Text style={{color: '#ffffff'}}>Auth </Text>
    </View>
  );
};

export default AuthScreen;
